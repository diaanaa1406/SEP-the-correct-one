public class ExamTest
//file to see if everything is working well. -Javier
{
  public static void main(String[] args){
  /* MyDate d = new MyDate(3, 3, 3);
  Teacher t = new Teacher("Alan", "A");
  Course co = new Course("a", "b", t, 33, "c");
  Classroom cl = new Classroom("a", 2, true, 4);
    Clock c1 = new Clock(9, 0);
    Clock c2 = new Clock(12, 0);
    t.addAvailability(d);
    TeacherList teacherl = new TeacherList();
    teacherl.addTeacher(t);
    CourseList coursel = new CourseList();
    coursel.addCourse(co);
    ClassroomList classl = new ClassroomList();
    classl.addClassroom(cl);
  Exam ex = new Exam("hey", d, c1, c2, classl, coursel, teacherl);
    System.out.println(ex.toString());*/
    System.out.println("hi");
  }
}
